# MYND Brain - Environment Configuration
# ========================================
# Copy this file to .env and customize for your deployment
#
# For Runpod GPU deployment:
#   - Set MYND_BRAIN_URL to your Runpod public URL
#   - Increase WHISPER_MODEL to "medium" or "large" for better accuracy
#   - TORCH_DEVICE will auto-detect CUDA

# Server Settings
MYND_BRAIN_PORT=8420
MYND_BRAIN_HOST=0.0.0.0

# Device Selection (auto-detect if not set)
# Options: cuda, mps, cpu
# TORCH_DEVICE=cuda

# Model Settings
# Embedding model for text similarity
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Whisper model for voice transcription
# Options: tiny, base, small, medium, large, large-v2, large-v3
# Larger models are more accurate but require more VRAM
WHISPER_MODEL=base

# CLIP model for image understanding
CLIP_MODEL=ViT-B-32
CLIP_PRETRAINED=laion2b_s34b_b79k

# Graph Transformer settings
GRAPH_HIDDEN_DIM=512
GRAPH_NUM_HEADS=8
GRAPH_NUM_LAYERS=3

# Context Lens freshness (seconds)
CONTEXT_LENS_FRESHNESS=300

# Claude CLI Settings
# The CLI uses your Claude Max subscription - no API key needed
# Authentication is stored in ~/.claude/credentials.json
# ANTHROPIC_API_KEY= (leave empty to force CLI mode)
